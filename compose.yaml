services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=employee_management'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
